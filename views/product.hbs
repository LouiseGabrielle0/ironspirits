<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./css/style.css" />
    <title>Iron Spirits</title>
  </head>
  <body>
      <nav>
          <ul>
              <li>
                  <a href="/">Home</a>
              </li>
              <li>
                  <a href="/about">About</a>
              </li>
              <li>
                  <a href="/contact">Contact</a>
              </li>
          </ul>
      </nav>
    <h1>Product: {{title}}</h1>
    <h2>Price: £{{price}}</h2>
   
</P>


{{#if imageFile }}
<img src="/images/{{imageFile}}" alt="{{title}}" />
{{else}}
<img src="/images/placeholder" alt="{{title}}" />
{{/if}}
 
 <section>
     <h5>
         This product is available in the following sizes:
     </h5>
     <ul>
         {{#if sizes}}
         {{#each sizes}}
         <li>             
             <a href="#">{{this}}</a>
         </li>
         <li>
         {{/each}}
         {{else}}
          <li>
             <a href="#">None available</a>
         </li>
         {{/if}}
     </ul>
 </section>

  </body>
</html>